{
  "type": "module",
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ],
    "testMatch": [
      "**/__tests__/**/*.js"
    ]
  },
  "scripts": {
    "dev": "node --watch-path=controllers --watch-path=routes --watch-path=services --watch-path=models --watch-path=middleware --watch-path=validators --watch-path=config --watch-path=utils --watch-path=server.js server.js",
    "seed:demo": "node scripts/seed-demo-user.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.6.0",
    "mongoose": "^9.9.4",
    "multer": "^2.3.0",
    "node": "^26.8.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.5.0",
    "supertest": "^7.2.2"
  }
}